apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "flask-to-learn-containers.fullname" . }}-test-connection"
  labels:
    {{- include "flask-to-learn-containers.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "flask-to-learn-containers.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
